apiVersion: v1
kind: Service
metadata:
  name: {{ include "life-journal.fullname" . }}
  labels:
    {{- include "life-journal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "life-journal.selectorLabels" . | nindent 4 }}

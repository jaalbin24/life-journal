apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "life-journal.fullname" . }}-test-connection"
  labels:
    {{- include "life-journal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "life-journal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

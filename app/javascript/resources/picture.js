export default class Picture {
    constructor(id, fileUrl, title, description) {
        this.id = id;
        this.fileUrl = fileUrl;
        this.title = title;
        this.description = description;
    }
}
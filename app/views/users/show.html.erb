<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>
    
    <%= link_to 'Edit this user', edit_user_path(current_user), class: "btn-secondary" %>
    <div class="inline-block ml-2">
      <%= button_to 'Destroy this user', user_path(current_user), method: :delete, class: "btn-secondary" %>
    </div>
    <%= link_to 'Back to users', users_path, class: "btn-secondary" %>
  </div>
</div>

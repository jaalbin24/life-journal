<div class="w-full min-h-full flex justify-center">
  <div class="w-2/3 p-5 flex flex-col gap-2">
    <div class="">
      <%= render "header_member" %>
    </div>
    <div class="grow">
      <%= render "layouts/tab_bar_member", model: @user, tabs: [:account], active_tab: @tab %>
      <div class="p-3 h-full">
        <% case @tab %>
        <% when 0 %>
        <% else %>
          <%= render "account" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New entry</h1>

  <%= render "form", entry: @entry %>

  <%= link_to 'Back to entries', entries_path, class: "btn-secondary" %>
</div>

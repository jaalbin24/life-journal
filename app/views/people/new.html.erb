<div class="w-full min-h-full flex justify-center">
  <div class="w-2/3 p-5 flex flex-col gap-2">
    <div class="">
      <%= render "header_member" %>
    </div>
    <div class="grow">
      <%= render "layouts/tab_bar_member", model: @person, tabs: [:info, :biography, :notes, :mentions], active_tab: :info %>
      <div class="p-3 h-full">
        <%= render "form" %>
      </div>
    </div>
  </div>
</div>
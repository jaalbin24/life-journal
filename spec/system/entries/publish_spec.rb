RSpec.describe "Publishing an entry" do
    before do
        # Create a user and sign it in
    end
    context "without any details" do
        it "will show a flash alert message" do
            pending "Test can be written"
            fail
        end
        it "will not publish the entry" do
            pending "Test can be written"
            fail
        end
    end
    it "will show a flash success message" do
        pending "Test can be written"
        fail
    end
    it "will publish the entry" do
        pending "Test can be written"
        fail
    end
    it "will show the entry show page" do
        pending "Test can be written"
        fail
    end
end